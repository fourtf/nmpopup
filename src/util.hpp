#pragma once

#define continue_if(exp)                                                       \
  if (exp)                                                                     \
  continue

#define return_if(exp)                                                         \
  if (exp)                                                                     \
  return
